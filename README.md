# Telegram Channel Aggregator Bot 🤖

**Telegram-бот для агрегации и автоматической суммаризации сообщений из Telegram-каналов**

## ✅ Последние исправления (30.01.2026)

### Исправленные проблемы!!!:
- **requirements.txt** - Восстановлен файл с правильными зависимостями и кодировкой
- **summarizer.py** - Полностью переписан с улучшенной обработкой ошибок
- **Импорты** - Добавлена обработка отсутствующих зависимостей
- **Конфигурация** - Создан .env.example с примерами настроек
- **Тестирование** - Добавлены скрипты проверки проекта

### Новые файлы:
- `.env.example` - Пример файла настроек
- `test_project.py` - Скрипт для тестирования проекта  
- `check_syntax.py` - Проверка синтаксиса Python файлов

## Описание

Бот автоматически собирает сообщения с подписанных каналов, генерирует краткие резюме и отправляет их пользователю в удобном формате. Это позволяет экономить время на чтение большого количества каналов.

## Функциональность

### Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и начало работы |
| `/channels` | Показать список отслеживаемых каналов |
| `/subscribe <channel>` | Подписаться на канал |
| `/unsubscribe <channel>` | Отписаться от канала |
| `/settings` | Настройки суммаризации |
| `/digest` | Срочная выдача дайджеста |
| `/help` | Показать справку |

### Автоматические функции

- **Планировщик** — периодическая проверка новых сообщений (настраивается)
- **Суммаризация** — генерация краткого содержания сообщений
- **Поддержка медиа** — обработка изображений при суммаризации
- **Хранение истории** — сохранение обработанных сообщений в базе данных

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd telegram-aggregator
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Настройте окружение. Создайте файл `.env` и заполните его:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token

# Telegram API (для Telethon)
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash

# OpenAI API (опционально, для облачной суммаризации)
OPENAI_API_KEY=your_openai_key

# База данных
DATABASE_URL=sqlite:///telegram_aggregator.db
```

### Получение API-ключей

1. **Telegram Bot Token:**
   - Напишите [@BotFather](https://t.me/BotFather) в Telegram
   - Создайте нового бота и получите токен

2. **Telegram API Credentials:**
   - Перейдите на [my.telegram.org](https://my.telegram.org)
   - Создайте приложение для получения `API_ID` и `API_HASH`

3. **OpenAI API Key (опционально):**
   - Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
   - Получите API-ключ в разделе API Keys

## Запуск

```bash
python main.py
```

## Структура проекта

```
telegram-aggregator/
├── main.py              # Точка входа, инициализация бота
├── bot.py               # Обработчики команд бота
├── scraper.py           # Сбор сообщений через Telethon
├── summarizer.py        # Суммаризация текста
├── scheduler.py         # Планировщик проверки сообщений
├── database.py          # Работа с базой данных
├── models.py            # Модели данных SQLAlchemy
├── config.py            # Конфигурация приложения
├── requirements.txt     # Зависимости проекта
└── README.md            # Документация
```

## Зависимости

- `python-telegram-bot` — Telegram Bot API
- `telethon` — MTProto для доступа к Telegram
- `apscheduler` — Планировщик задач
- `sqlalchemy` — ORM для работы с БД
- `openai` — OpenAI API для суммаризации
- `transformers` — Локальные модели для суммаризации
- `torch` — PyTorch для работы моделей
- `python-dotenv` — Переменные окружения
- `Pillow` — Обработка изображений

## Архитектура

```
Пользователь → Telegram Bot → Message Processor → Summarizer → Storage → Notifier
                              ↑
                      Channel Scrapers (MTProto)
```

## Безопасность

- Токены и API-ключи хранятся в переменных окружения
- Сессии Telethon защищены
- Данные остаются на сервере пользователя
- Обработка ошибок RateLimitError от Telegram API

## Лицензия

MIT License