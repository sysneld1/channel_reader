# Инструкция по настройке .env файла

## Шаг 1: Получение API credentials от Telegram

### Получение BOT_TOKEN:
1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен бота и вставьте в `BOT_TOKEN`

### Получение API_ID и API_HASH:
1. Перейдите на https://my.telegram.org
2. Войдите в свой аккаунт Telegram
3. Перейдите в "API development tools"
4. Нажмите "Create application"
5. Заполните форму:
   - App title: название вашего приложения
   - Short name: короткое название
   - Platform: выберите подходящую платформу
6. После создания скопируйте:
   - `api_id` (число) → вставьте в `API_ID`
   - `api_hash` (строка) → вставьте в `API_HASH`

## Шаг 2: Настройка переменных в .env файле

Откройте файл `.env` и замените следующие значения:

```env
# Обязательные настройки
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
API_ID=12345678
API_HASH=your_32_character_api_hash_here

# Дополнительные настройки (опционально)
SUMMARIZATION_TYPE=short
CHECK_INTERVAL_SECONDS=10
DATABASE_URL=sqlite:///./chanel_reader.db
```

## Шаг 3: Запуск приложения

После настройки .env файла запустите приложение:

```bash
python main.py
```

При первом запуске программа попросит:
1. Номер телефона (в формате +79991234567)
2. Код подтверждения из Telegram

После авторизации сессия сохранится и не потребует повторного ввода.

## Важные замечания

### 🔒 Безопасность
- **Никогда не делитесь API credentials** с другими людьми
- **Файл `.env` уже добавлен в `.gitignore`** и автоматически исключается из Git
- **Используйте разные credentials** для разработки и продакшена
- Если API_ID = 0, проверьте правильность настройки в .env файле

### ✅ Проверка Git статуса
Чтобы убедиться, что .env файл не попадет в коммиты:
```bash
git status
# .env файл не должен появляться в списке изменений
```

## Дополнительные настройки

### Суммаризация текста:
- `short` - быстрая обрезка текста (рекомендуется для начала)
- `local` - локальная FLAN-T5 модель (требует установки)
- `api` - OpenAI/Gemini API (требует API ключ)
- `llama_cpp` - локальная GGUF модель (требует модель)

### Интервал проверки:
- Минимум: 30 секунд
- Рекомендуемое: 60-300 секунд
- Слишком частые проверки могут привести к блокировке Telegram