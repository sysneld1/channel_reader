# Быстрый запуск Channel Reader

## ✅ Готово к работе!

Файл `.env` создан и настроен со всеми необходимыми параметрами.

## 🔒 Важно: Безопасность

**Файл `.env` уже добавлен в `.gitignore` и НЕ будет попадать в GitHub!**

### ✅ Исправлено: Структура .gitignore
- **Один файл `.gitignore`** в корне проекта (правильно)
- **НЕТ файлов `.gitignore`** в папке `.venv` (избыточно)
- **Полная защита** всех чувствительных данных

## Шаги для запуска:

1. **Проверьте конфигурацию:**
   ```bash
   python check_config.py
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Запустите приложение:**
   ```bash
   python main.py
   ```

## При первом запуске:

Программа попросит:
- Номер телефона (например: +79991234567)
- Код подтверждения из Telegram

После этого бот будет готов к работе!

## Команды бота:

- `/start` - начать работу
- `/channels` - список каналов  
- `/subscribe <канал>` - подписаться на канал
- `/unsubscribe <канал>` - отписаться
- `/settings` - настройки
- `/digest` - срочный дайджест

## Готово! 

Все настройки проверены и готовы к использованию. 🚀